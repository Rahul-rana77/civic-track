{
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "start": "node backend/server.js",
    "build": "vite build"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  }
}
